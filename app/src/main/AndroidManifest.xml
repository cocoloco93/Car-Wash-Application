<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carwash">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/MyanmarFontTheme" >
        </activity>
        <activity android:name=".ui.auth.SignInActivity" />
        <activity android:name=".ui.auth.SignUpActivity" />
        <activity android:name=".ui.gettingstart.OtherItemCreation" />
        <activity android:name=".ui.gettingstart.ManagerCreation" />
        <activity android:name=".ui.gettingstart.ShopCreation">



        </activity>
        <activity android:name=".ui.gettingstart.ServiceCreation" />
        <activity android:name=".GettingStartActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>